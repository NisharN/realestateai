# Core Framework
fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
email-validator==2.2.0
python-dotenv==1.0.1

# Database
supabase==2.30.0
asyncpg==0.29.0

# LLM / STT providers (all behind app/modules/llm/gateway.py)
groq>=0.9.0
huggingface-hub>=0.34.4

# Data cleaning
phonenumbers>=8.13
pyyaml>=6.0

# Data Processing
pandas==2.2.0
numpy==1.26.0

# Voice / Audio
# piper-tts (subprocess binary, not pip-installable). Used when installed locally.
webrtcvad-wheels==2.0.14

# WebSocket / Real-time
websockets>=12,<16
python-socketio==5.11.0

# HTTP / API
httpx>=0.27,<1
aiohttp==3.9.0
requests==2.31.0

# Utilities
python-multipart==0.0.9

# Background jobs (app/worker.py); docker-compose declares worker + beat.
celery==5.4.0
redis==5.0.7

# Billing
stripe==10.4.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.8
