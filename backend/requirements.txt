# Core Framework
fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.7.0
pydantic-settings==2.3.4
email-validator==2.2.0
python-dotenv==1.0.1

# Database
supabase==2.30.0
asyncpg==0.29.0

# AI / LLM
langchain==0.2.0
langchain-core==0.2.0
# 0.1.0 requires langchain-core <0.2 and conflicts with the 0.2 stack below.
langchain-groq==0.1.5
groq==0.9.0
huggingface-hub==0.34.4
langgraph==0.0.59

# Scraping
playwright==1.44.0
scrapy==2.11.0
fake-useragent==1.5.0

# Data Processing
pandas==2.2.0
numpy==1.26.0

# Voice / Audio
# piper-tts (subprocess binary, not pip-installable). Used when installed locally.
gTTS==2.5.1
webrtcvad==2.0.10

# WebSocket / Real-time
websockets==15.0.1
python-socketio==5.11.0

# HTTP / API
httpx==0.27.0
aiohttp==3.9.0
requests==2.31.0

# Utilities
python-multipart==0.0.9

# Background jobs. Celery runs scheduled property ingestion and the reminders
# engine (app/worker.py); docker-compose declares the worker and beat services
# that execute them.
celery==5.4.0
redis==5.0.7

# Billing
stripe==10.4.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.8
